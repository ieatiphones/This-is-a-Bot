const Discord = module.require("discord.js");
const config = module.require('../config.json');
const fs = require('fs');

exports.apply = function (path) {
    
}

exports.info = {
    name: 'name'
};